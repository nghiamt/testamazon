<div id="user">
<h1>Editing user</h1>

<%= render 'form' %>

<%= link_to 'Show', @user %> |
<% if session[:user_id]%>
<% if User.find_by_id(session[:user_id]).is_admin%>
<%= link_to 'Back', users_path %>

<%else%>
<%= link_to 'Back', @user %>
<%end%>
<%else%>
<%= link_to 'Login', login_path %>
<%end%>
</div>