<div id="user">
<h1>New order</h1>

<%= render 'form' %>

<% if session[:user_id]%>
<% if User.find_by_id(session[:user_id]).is_admin%>
<%= link_to 'Back', orders_path %>
<%else%>
<%= link_to 'Back', root_path %>
<%end%>
<%end%>
</div>